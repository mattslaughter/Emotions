<!DOCTYPE html>
<!--<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>MY APP</title>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <h1>May Node and Express be with you.</h1>

  <ul class="quotes">

  </ul>

  <h2>Add a quote</h2>

  <form action="/quotes" method="POST">
    <input type="text" placeholder="name" name="name">
    <input type="text" placeholder="quote" name="quote">
    <button type="submit">Submit</button>
  </form>

  <div>
    <h2>Replace last quote written by Master Yoda with a quote written by Darth Vadar</h2>
    <button id="update"> Darth Vadar invades! </button>
  </div>

  <div>
    <h2>Delete Darth Vadar's first quote</h2>
    <button id="delete"> Delete first Darth Vadar quote </button>
  </div>

  <script src="main.js"></script>

</body>
</html>
-->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" media="screen" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.3.5/css/bootstrap.min.css">
    <title>Emotional Checkin</title>
    <!--  -->
    <style>
        .pod {
            padding: 10px;
        }

        .box h1,
        h2,
        h3,
        h4,
        strong {
            text-align: center;
        }

        .box {
            box-shadow: 2px 2px 2px 2px #777;
            background: #FFF;
            margin: 40px auto;
            padding: 10px;
        }

        .btn-group-wrap {
            text-align: center;
        }

        div.btn-group {}

        a {
            float: left;
        }

        .topdiv {
            margin-top: 70px;
        }
    </style>
</head>
<!--
        ID wants desires pleasure/pain
        superego questioner judge
        ego reality others
        Conscious Ego - What others and that person are aware of in that individual making decisions.
        Personal Unconscious - Part of the person that they are unaware of.
        Collective Unconscious - Shared memories of all mankind.
        There are also four functions of the mind in each person. Thinking (solving problems) vs. Feeling (emotions). Sensing (Do you feel something there?) vs. Intuiting (Asking questions about a subject).
        -->
<body>
    <div class="container">
        <div class="row">
            <div class="jumbotron topdiv">
            </div>
            <div id="disp"></div>
            <div id="emoView">
            </div>
            <form name="adduser",method="post",action="/adduser">
    <button class="btn btn-lg" type="submit">test</button>
            </form>
        </div>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.3.5/js/bootstrap.min.js"></script>
    <script src="main.js"></script>
    <script>
        //extends string function so you can cap the first letter of a string
        String.prototype.capitalize = function () {
            return this.charAt(0).toUpperCase() + this.slice(1);
        }
        //-------------------------------------------
        //new instance of class constructor in script.js
        var emo = new Emotions();
        //init function
        function initi() {
            //example how to call function in array
            //  var x = emo.emoArray[0]();
            //emo.mind();
            emo.buildEmoView();
            //populates the array with current values
            getValues();
        }
        initi();
        //creates a loop
        //        setInterval(function() {
        //        emo.mind();
        //        }, emo.interval );
    </script>
</body>
</html>
